server:
  port: 8080

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    name: poot
    password: root
    url: jdbc:mysql://127.0.0.1:3306/shiro?useSSL=false&useUnicode=true&characterEncoding=UTF-8&serverTimezone=UTC&allowPublicKeyRetrieval=true

mybatis:
  type-aliases-package: com.demo.vchat.vchat.domain

springdoc:
  swagger-ui:
    path: /swagger-ui.html
